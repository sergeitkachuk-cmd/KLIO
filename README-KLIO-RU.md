# КЛИО — последняя версия проекта

Архив подготовлен 2 августа 2026 года из актуальной опубликованной версии сайта КЛИО.

В состав входят:

- публичная презентационная страница;
- личный кабинет и переключение кабинетов брендов;
- генератор материалов;
- профиль бренда;
- модуль семантики;
- анализ конкурентов;
- контент-план с выборочной заменой тем;
- раздел «Материалы» с версиями результатов;
- редакторы КЛИО;
- раздельные лимиты материалов, исследований и AI-редактуры;
- адаптивный интерфейс для телефонов, планшетов и компьютеров;
- серверные API-маршруты и структура базы данных.

Мобильная версия не является отдельным сайтом: она реализована в тех же исходниках через адаптивные стили и компоненты. Поэтому все размеры экрана поддерживаются одной кодовой базой.

## Системные требования

- Node.js 22.13 или новее;
- npm;
- ключ OpenAI API для работы AI-функций.

## Локальный запуск

1. Откройте папку проекта в терминале.
2. Выполните `npm ci`.
3. Создайте файл `.env.local` на основе `.env.example`.
4. Укажите в нём `OPENAI_API_KEY`. Модель выбирать не нужно — маршрутизатор
   в `app/api/_lib/ai-config.ts` сам решает между `gpt-5.6-luna` и
   `gpt-5.4-nano` для каждой операции.
5. Выполните `npm run dev`.

## Проверка и сборка

- `npm test` — сборка и функциональные проверки;
- `npm run lint` — проверка исходного кода;
- `npm run build` — производственная сборка.

## Развёртывание

Основное Next.js-приложение и его база данных сейчас разворачиваются на
Timeweb App Platform из `main` через корневой `Dockerfile`. Render больше не
используется для полного KLIO: там оставлен отдельный relay-сервис
`klio-telegram-relay.onrender.com`, который обслуживает Telegram и серверную
генерацию изображений/GPT.

Старый Blueprint полного приложения сохранён в `render.yaml.legacy` только для
истории и не должен подключаться в Render. Если нужен новый Render-сервис,
сначала сверяйтесь с `services/klio-images/COMBINED.md`: создание второго
сервиса изображений не является текущей рабочей схемой.

Для основного приложения переменные `DATABASE_URL`, ключи AI, почты, оплаты,
S3 и relay задаются в настройках Timeweb. На Render меняются только настройки
relay и его секреты; секрет `KLIO_IMAGE_SERVICE_TOKEN` должен совпадать с
переменной основного приложения `KLIO_IMAGE_SERVICE_TOKEN`.

Личный кабинет открыт для любого посетителя: регистрация и вход — по email
и паролю (`/signup`, `/login`), без общего пароля на весь сайт. Пароли
хранятся хешированными (scrypt), сессия — httpOnly-cookie, привязанная к
записи в таблице `sessions`. При открытии внутри ChatGPT как App по-прежнему
работает вход через заголовок ChatGPT — оба способа используют один и тот же
личный кабинет по email.

После регистрации доступ в кабинет заблокирован, пока не подтверждён email:
на адрес уходит письмо со ссылкой (действует 24 часа), без перехода по ней
войти нельзя — это отсекает ботов и опечатки в адресе. Письма отправляются
через [Resend](https://resend.com); нужен ключ `RESEND_API_KEY` (секрет,
задаётся в Timeweb или `.env.local`). `RESEND_FROM_EMAIL` можно не менять —
по умолчанию используется тестовый адрес Resend `onboarding@resend.dev`,
который отправляет письма без подтверждения собственного домена, но **только
на email владельца аккаунта Resend** — остальным адресатам Resend откажет,
пока не подключён и не подтверждён свой домен в панели Resend. Также нужен
`APP_BASE_URL` с адресом основного сайта — он используется для ссылки
подтверждения в письме.
Дополнительно `/api/auth/signup`,
`/login` и `/resend-verification` ограничены по частоте запросов с одного
IP (защита от перебора).

Для локальной проверки основного приложения используйте:

- `npm ci`;
- `npm run db:push` при настроенном `DATABASE_URL`;
- `npm run build`;
- `npm start`.

## Важно

- Реальные API-ключи в архив не включены.
- Папки зависимостей, временные файлы и локальные служебные данные исключены.
- Файлы готовы для размещения в Git-репозитории.
- Исходное состояние сайта: версия 32, базовый коммит `553679d`.
- В пакет добавлена только одна техническая корректировка теста интерфейса; она не меняет работу или внешний вид сайта.

Опубликованная версия: https://textora-studio.serg-ptz.chatgpt.site
