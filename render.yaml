services:
  - type: web
    name: klio
    runtime: node
    plan: starter
    branch: main
    buildCommand: npm ci && npm run build
    preDeployCommand: npm run db:push
    startCommand: npm start
    healthCheckPath: /api/ai-status
    envVars:
      - key: NODE_VERSION
        value: 22.13.0
      - key: DATABASE_URL
        fromDatabase:
          name: klio-db
          property: connectionString
      - key: OPENAI_API_KEY
        sync: false
      - key: OPENAI_MODEL
        value: gpt-5.6-terra
      - key: OPENAI_SEARCH_MODEL
        value: gpt-5.6-terra
      - key: RESEND_API_KEY
        sync: false
      - key: RESEND_FROM_EMAIL
        value: "KLIO <onboarding@resend.dev>"
      - key: APP_BASE_URL
        value: https://klio-g1eg.onrender.com

databases:
  - name: klio-db
    plan: free
